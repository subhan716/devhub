# Installed Libraries

## Frontend
- `emoji-picker-react`: Added for rendering the emoji keyboard in chat components (`MessagesPage.jsx` and `FloatingChat.jsx`).

## Backend
- `cloudinary` (v2): Integrated for handling all file and media uploads.
- `multer-storage-cloudinary`: Used as the storage engine for Multer to upload directly to Cloudinary.
